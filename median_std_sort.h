#pragma once

#ifndef MEDIAN_STD_SORT_H
#define MEDIAN_STD_SORT_H
#include <iostream>
#include <vector>
#include <chrono>
#include <algorithm>

int median_std_sort(std::vector<int> random_set);

#endif
