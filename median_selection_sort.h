#pragma once

#ifndef MEDIAN_SELECTION_SORT_H
#define MEDIAN_SELECTION_SORT_H
#include <iostream>
#include <vector>
#include <chrono>

int median_selection_sort(std::vector<int> random_set);

#endif